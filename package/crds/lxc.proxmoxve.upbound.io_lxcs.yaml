---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: lxcs.lxc.proxmoxve.upbound.io
spec:
  group: lxc.proxmoxve.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - proxmoxve
    kind: Lxc
    listKind: LxcList
    plural: lxcs
    singular: lxc
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Lxc is the Schema for the Lxcs API. Provides a resource to manage
          LXC
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LxcSpec defines the desired state of Lxc
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicy field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  arch:
                    description: Sets the container OS architecture type. Default
                      is "amd64".
                    type: string
                  bwlimit:
                    description: A number for setting the override I/O bandwidth limit
                      (in KiB/s).
                    type: number
                  clone:
                    description: The lxc vmid to clone
                    type: string
                  cloneStorage:
                    description: Target storage for full clone.
                    type: string
                  cmode:
                    description: Configures console mode. "tty" tries to open a connection
                      to one of the available tty devices. "console" tries to attach
                      to /dev/console instead. "shell" simply invokes a shell inside
                      the container (no login). Default is "tty".
                    type: string
                  console:
                    description: A boolean to attach a console device to the container.
                      Default is true.
                    type: boolean
                  cores:
                    description: The number of cores assigned to the container. A
                      container can use all available cores by default.
                    type: number
                  cpulimit:
                    description: A number to limit CPU usage by. Default is 0.
                    type: number
                  cpuunits:
                    description: A number of the CPU weight that the container possesses.
                      Default is 1024.
                    type: number
                  description:
                    description: Sets the container description seen in the web interface.
                    type: string
                  features:
                    description: An object for allowing the container to access advanced
                      features.
                    items:
                      properties:
                        fuse:
                          description: A boolean for enabling FUSE mounts.
                          type: boolean
                        keyctl:
                          description: A boolean for enabling the keyctl() system
                            call.
                          type: boolean
                        mknod:
                          type: boolean
                        mount:
                          description: Defines the filesystem types (separated by
                            semicolons) that are allowed to be mounted.
                          type: string
                        nesting:
                          description: A boolean to allow nested virtualization.
                          type: boolean
                      type: object
                    type: array
                  force:
                    description: A boolean that allows the overwriting of pre-existing
                      containers.
                    type: boolean
                  full:
                    description: When cloning, create a full copy of all disks. This
                      is always done when you clone a normal CT. For CT template it
                      creates a linked clone by default.
                    type: boolean
                  hagroup:
                    description: The HA group identifier the resource belongs to (requires
                      hastate to be set!). See the docs about HA for more info.
                    type: string
                  hastate:
                    description: Requested HA state for the resource. One of "started",
                      "stopped", "enabled", "disabled", or "ignored". See the docs
                      about HA for more info.
                    type: string
                  hookscript:
                    description: A string containing a volume identifier to a script
                      that will be executed during various steps throughout the container's
                      lifetime. The script must be an executable file.
                    type: string
                  hostname:
                    description: Specifies the host name of the container.
                    type: string
                  ignoreUnpackErrors:
                    description: A boolean that determines if template extraction
                      errors are ignored during container creation.
                    type: boolean
                  lock:
                    description: A string for locking or unlocking the VM.
                    type: string
                  memory:
                    description: A number containing the amount of RAM to assign to
                      the container (in MB).
                    type: number
                  mountpoint:
                    description: An object for defining a volume to use as a container
                      mount point. Can be specified multiple times.
                    items:
                      properties:
                        acl:
                          description: A boolean for enabling ACL support. Default
                            is false.
                          type: boolean
                        backup:
                          description: A boolean for including the mount point in
                            backups. Default is false.
                          type: boolean
                        file:
                          type: string
                        key:
                          description: The number that identifies the mount point
                            (i.e. the n in mp[n]).
                          type: string
                        mp:
                          description: The path to the mount point as seen from inside
                            the container. The path must not contain symlinks for
                            security reasons.
                          type: string
                        quota:
                          description: A boolean for enabling user quotas inside the
                            container for this mount point. Default is false.
                          type: boolean
                        replicate:
                          description: A boolean for including this volume in a storage
                            replica job. Default is false.
                          type: boolean
                        shared:
                          description: A boolean for marking the volume as available
                            on all nodes. Default is false.
                          type: boolean
                        size:
                          description: Size of the underlying volume. Must end in
                            T, G, M, or K (e.g. "1T", "1G", "1024M" , "1048576K").
                            Note that this is a read only value.
                          type: string
                        slot:
                          description: A string containing the number that identifies
                            the mount point (i.e. the n in mp[n]).
                          type: number
                        storage:
                          description: A string containing the volume , directory,
                            or device to be mounted into the container (at the path
                            specified by mp). E.g. local-lvm, local-zfs, local etc.
                          type: string
                        volume:
                          type: string
                      required:
                      - key
                      - mp
                      - size
                      - slot
                      - storage
                      type: object
                    type: array
                  nameserver:
                    description: The DNS server IP address used by the container.
                      If neither nameserver nor searchdomain are specified, the values
                      of the Proxmox host will be used by default.
                    type: string
                  network:
                    description: An object defining a network interface for the container.
                      Can be specified multiple times.
                    items:
                      properties:
                        bridge:
                          description: The bridge to attach the network interface
                            to (e.g. "vmbr0").
                          type: string
                        firewall:
                          description: A boolean to enable the firewall on the network
                            interface.
                          type: boolean
                        gw:
                          description: The IPv4 address belonging to the network interface's
                            default gateway.
                          type: string
                        gw6:
                          description: The IPv6 address of the network interface's
                            default gateway.
                          type: string
                        hwaddr:
                          description: A string to set a common MAC address with the
                            I/G (Individual/Group) bit not set. Automatically determined
                            if not set.
                          type: string
                        ip:
                          description: The IPv4 address of the network interface.
                            Can be a static IPv4 address (in CIDR notation), "dhcp",
                            or "manual".
                          type: string
                        ip6:
                          description: The IPv6 address of the network interface.
                            Can be a static IPv6 address (in CIDR notation), "auto"
                            , "dhcp", or "manual".
                          type: string
                        mtu:
                          description: A string to set the MTU on the network interface.
                          type: number
                        name:
                          description: The name of the network interface as seen from
                            inside the container (e.g. "eth0").
                          type: string
                        rate:
                          description: A number that sets rate limiting on the network
                            interface (Mbps).
                          type: number
                        tag:
                          description: A number that specifies the VLAN tag of the
                            network interface. Automatically determined if not set.
                          type: number
                        trunks:
                          type: string
                        type:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  onboot:
                    description: A boolean that determines if the container will start
                      on boot. Default is false.
                    type: boolean
                  ostemplate:
                    description: The volume identifier that points to the OS template
                      or backup file.
                    type: string
                  ostype:
                    description: The operating system type, used by LXC to set up
                      and configure the container. Automatically determined if not
                      set.
                    type: string
                  passwordSecretRef:
                    description: Sets the root password inside the container.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  pool:
                    description: The name of the Proxmox resource pool to add this
                      container to.
                    type: string
                  protection:
                    description: A boolean that enables the protection flag on this
                      container. Stops the container and its disk from being removed/updated.
                      Default is false.
                    type: boolean
                  restore:
                    description: A boolean to mark the container creation/update as
                      a restore task.
                    type: boolean
                  rootfs:
                    description: An object for configuring the root mount point of
                      the container. Can only be specified once.
                    items:
                      properties:
                        acl:
                          description: A boolean for enabling ACL support. Default
                            is false.
                          type: boolean
                        quota:
                          description: A boolean for enabling user quotas inside the
                            container for this mount point. Default is false.
                          type: boolean
                        replicate:
                          description: A boolean for including this volume in a storage
                            replica job. Default is false.
                          type: boolean
                        ro:
                          type: boolean
                        shared:
                          description: A boolean for marking the volume as available
                            on all nodes. Default is false.
                          type: boolean
                        size:
                          description: Size of the underlying volume. Must end in
                            T, G, M, or K (e.g. "1T", "1G", "1024M" , "1048576K").
                            Note that this is a read only value.
                          type: string
                        storage:
                          description: A string containing the volume , directory,
                            or device to be mounted into the container (at the path
                            specified by mp). E.g. local-lvm, local-zfs, local etc.
                          type: string
                      required:
                      - size
                      - storage
                      type: object
                    type: array
                  searchdomain:
                    description: Sets the DNS search domains for the container. If
                      neither nameserver nor searchdomain are specified, the values
                      of the Proxmox host will be used by default.
                    type: string
                  sshPublicKeys:
                    description: Multi-line string of SSH public keys that will be
                      added to the container. Can be defined using heredoc syntax.
                    type: string
                  start:
                    description: A boolean that determines if the container is started
                      after creation. Default is false.
                    type: boolean
                  startup:
                    description: The startup and shutdown behaviour of the container.
                    type: string
                  swap:
                    description: A number that sets the amount of swap memory available
                      to the container. Default is 512.
                    type: number
                  tags:
                    description: Tags of the container. This is only meta information.
                    type: string
                  targetNode:
                    description: A string containing the cluster node name.
                    type: string
                  template:
                    description: A boolean that determines if this container is a
                      template.
                    type: boolean
                  tty:
                    description: A number that specifies the TTYs available to the
                      container. Default is 2.
                    type: number
                  unique:
                    description: A boolean that determines if a unique random ethernet
                      address is assigned to the container.
                    type: boolean
                  unprivileged:
                    description: A boolean that makes the container run as an unprivileged
                      user. Default is false.
                    type: boolean
                  vmid:
                    description: A number that sets the VMID of the container. If
                      set to 0, the next available VMID is used. Default is 0. The
                      VM identifier in proxmox (100-999999999)
                    type: number
                type: object
              managementPolicy:
                default: FullControl
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicy
                  specifies the level of control Crossplane has over the managed external
                  resource. This field is planned to replace the DeletionPolicy field
                  in a future release. Currently, both could be set independently
                  and non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - FullControl
                - ObserveOnly
                - OrphanOnDelete
                type: string
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: targetNode is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.targetNode)
          status:
            description: LxcStatus defines the observed state of Lxc.
            properties:
              atProvider:
                properties:
                  arch:
                    description: Sets the container OS architecture type. Default
                      is "amd64".
                    type: string
                  bwlimit:
                    description: A number for setting the override I/O bandwidth limit
                      (in KiB/s).
                    type: number
                  clone:
                    description: The lxc vmid to clone
                    type: string
                  cloneStorage:
                    description: Target storage for full clone.
                    type: string
                  cmode:
                    description: Configures console mode. "tty" tries to open a connection
                      to one of the available tty devices. "console" tries to attach
                      to /dev/console instead. "shell" simply invokes a shell inside
                      the container (no login). Default is "tty".
                    type: string
                  console:
                    description: A boolean to attach a console device to the container.
                      Default is true.
                    type: boolean
                  cores:
                    description: The number of cores assigned to the container. A
                      container can use all available cores by default.
                    type: number
                  cpulimit:
                    description: A number to limit CPU usage by. Default is 0.
                    type: number
                  cpuunits:
                    description: A number of the CPU weight that the container possesses.
                      Default is 1024.
                    type: number
                  description:
                    description: Sets the container description seen in the web interface.
                    type: string
                  features:
                    description: An object for allowing the container to access advanced
                      features.
                    items:
                      properties:
                        fuse:
                          description: A boolean for enabling FUSE mounts.
                          type: boolean
                        keyctl:
                          description: A boolean for enabling the keyctl() system
                            call.
                          type: boolean
                        mknod:
                          type: boolean
                        mount:
                          description: Defines the filesystem types (separated by
                            semicolons) that are allowed to be mounted.
                          type: string
                        nesting:
                          description: A boolean to allow nested virtualization.
                          type: boolean
                      type: object
                    type: array
                  force:
                    description: A boolean that allows the overwriting of pre-existing
                      containers.
                    type: boolean
                  full:
                    description: When cloning, create a full copy of all disks. This
                      is always done when you clone a normal CT. For CT template it
                      creates a linked clone by default.
                    type: boolean
                  hagroup:
                    description: The HA group identifier the resource belongs to (requires
                      hastate to be set!). See the docs about HA for more info.
                    type: string
                  hastate:
                    description: Requested HA state for the resource. One of "started",
                      "stopped", "enabled", "disabled", or "ignored". See the docs
                      about HA for more info.
                    type: string
                  hookscript:
                    description: A string containing a volume identifier to a script
                      that will be executed during various steps throughout the container's
                      lifetime. The script must be an executable file.
                    type: string
                  hostname:
                    description: Specifies the host name of the container.
                    type: string
                  id:
                    type: string
                  ignoreUnpackErrors:
                    description: A boolean that determines if template extraction
                      errors are ignored during container creation.
                    type: boolean
                  lock:
                    description: A string for locking or unlocking the VM.
                    type: string
                  memory:
                    description: A number containing the amount of RAM to assign to
                      the container (in MB).
                    type: number
                  mountpoint:
                    description: An object for defining a volume to use as a container
                      mount point. Can be specified multiple times.
                    items:
                      properties:
                        acl:
                          description: A boolean for enabling ACL support. Default
                            is false.
                          type: boolean
                        backup:
                          description: A boolean for including the mount point in
                            backups. Default is false.
                          type: boolean
                        file:
                          type: string
                        key:
                          description: The number that identifies the mount point
                            (i.e. the n in mp[n]).
                          type: string
                        mp:
                          description: The path to the mount point as seen from inside
                            the container. The path must not contain symlinks for
                            security reasons.
                          type: string
                        quota:
                          description: A boolean for enabling user quotas inside the
                            container for this mount point. Default is false.
                          type: boolean
                        replicate:
                          description: A boolean for including this volume in a storage
                            replica job. Default is false.
                          type: boolean
                        shared:
                          description: A boolean for marking the volume as available
                            on all nodes. Default is false.
                          type: boolean
                        size:
                          description: Size of the underlying volume. Must end in
                            T, G, M, or K (e.g. "1T", "1G", "1024M" , "1048576K").
                            Note that this is a read only value.
                          type: string
                        slot:
                          description: A string containing the number that identifies
                            the mount point (i.e. the n in mp[n]).
                          type: number
                        storage:
                          description: A string containing the volume , directory,
                            or device to be mounted into the container (at the path
                            specified by mp). E.g. local-lvm, local-zfs, local etc.
                          type: string
                        volume:
                          type: string
                      type: object
                    type: array
                  nameserver:
                    description: The DNS server IP address used by the container.
                      If neither nameserver nor searchdomain are specified, the values
                      of the Proxmox host will be used by default.
                    type: string
                  network:
                    description: An object defining a network interface for the container.
                      Can be specified multiple times.
                    items:
                      properties:
                        bridge:
                          description: The bridge to attach the network interface
                            to (e.g. "vmbr0").
                          type: string
                        firewall:
                          description: A boolean to enable the firewall on the network
                            interface.
                          type: boolean
                        gw:
                          description: The IPv4 address belonging to the network interface's
                            default gateway.
                          type: string
                        gw6:
                          description: The IPv6 address of the network interface's
                            default gateway.
                          type: string
                        hwaddr:
                          description: A string to set a common MAC address with the
                            I/G (Individual/Group) bit not set. Automatically determined
                            if not set.
                          type: string
                        ip:
                          description: The IPv4 address of the network interface.
                            Can be a static IPv4 address (in CIDR notation), "dhcp",
                            or "manual".
                          type: string
                        ip6:
                          description: The IPv6 address of the network interface.
                            Can be a static IPv6 address (in CIDR notation), "auto"
                            , "dhcp", or "manual".
                          type: string
                        mtu:
                          description: A string to set the MTU on the network interface.
                          type: number
                        name:
                          description: The name of the network interface as seen from
                            inside the container (e.g. "eth0").
                          type: string
                        rate:
                          description: A number that sets rate limiting on the network
                            interface (Mbps).
                          type: number
                        tag:
                          description: A number that specifies the VLAN tag of the
                            network interface. Automatically determined if not set.
                          type: number
                        trunks:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                  onboot:
                    description: A boolean that determines if the container will start
                      on boot. Default is false.
                    type: boolean
                  ostemplate:
                    description: The volume identifier that points to the OS template
                      or backup file.
                    type: string
                  ostype:
                    description: The operating system type, used by LXC to set up
                      and configure the container. Automatically determined if not
                      set.
                    type: string
                  pool:
                    description: The name of the Proxmox resource pool to add this
                      container to.
                    type: string
                  protection:
                    description: A boolean that enables the protection flag on this
                      container. Stops the container and its disk from being removed/updated.
                      Default is false.
                    type: boolean
                  restore:
                    description: A boolean to mark the container creation/update as
                      a restore task.
                    type: boolean
                  rootfs:
                    description: An object for configuring the root mount point of
                      the container. Can only be specified once.
                    items:
                      properties:
                        acl:
                          description: A boolean for enabling ACL support. Default
                            is false.
                          type: boolean
                        quota:
                          description: A boolean for enabling user quotas inside the
                            container for this mount point. Default is false.
                          type: boolean
                        replicate:
                          description: A boolean for including this volume in a storage
                            replica job. Default is false.
                          type: boolean
                        ro:
                          type: boolean
                        shared:
                          description: A boolean for marking the volume as available
                            on all nodes. Default is false.
                          type: boolean
                        size:
                          description: Size of the underlying volume. Must end in
                            T, G, M, or K (e.g. "1T", "1G", "1024M" , "1048576K").
                            Note that this is a read only value.
                          type: string
                        storage:
                          description: A string containing the volume , directory,
                            or device to be mounted into the container (at the path
                            specified by mp). E.g. local-lvm, local-zfs, local etc.
                          type: string
                        volume:
                          type: string
                      type: object
                    type: array
                  searchdomain:
                    description: Sets the DNS search domains for the container. If
                      neither nameserver nor searchdomain are specified, the values
                      of the Proxmox host will be used by default.
                    type: string
                  sshPublicKeys:
                    description: Multi-line string of SSH public keys that will be
                      added to the container. Can be defined using heredoc syntax.
                    type: string
                  start:
                    description: A boolean that determines if the container is started
                      after creation. Default is false.
                    type: boolean
                  startup:
                    description: The startup and shutdown behaviour of the container.
                    type: string
                  swap:
                    description: A number that sets the amount of swap memory available
                      to the container. Default is 512.
                    type: number
                  tags:
                    description: Tags of the container. This is only meta information.
                    type: string
                  targetNode:
                    description: A string containing the cluster node name.
                    type: string
                  template:
                    description: A boolean that determines if this container is a
                      template.
                    type: boolean
                  tty:
                    description: A number that specifies the TTYs available to the
                      container. Default is 2.
                    type: number
                  unique:
                    description: A boolean that determines if a unique random ethernet
                      address is assigned to the container.
                    type: boolean
                  unprivileged:
                    description: A boolean that makes the container run as an unprivileged
                      user. Default is false.
                    type: boolean
                  unused:
                    items:
                      type: string
                    type: array
                  vmid:
                    description: A number that sets the VMID of the container. If
                      set to 0, the next available VMID is used. Default is 0. The
                      VM identifier in proxmox (100-999999999)
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
